var chunlin9527 = {
    compact: function compact(ary) {  // 函数表达式的名字相当于函数内部的变量
        var result = []
        compact()
        for (var i = 0; i < ary.length; i++) {
            if (a[i]) {
                result.push(ary[i])
            }
        }
        return result
    }
}